import { BookOpenText, Sparkles } from 'lucide-react'
import Link from 'next/link'

export function AuthShell({ children, title, subtitle }: { children: React.ReactNode; title: string; subtitle: string }) {
  return <main className="relative flex min-h-screen items-center overflow-hidden bg-gradient-to-br from-blue-50 via-white to-orange-50 px-4 py-8 sm:px-6"><div className="pointer-events-none absolute -top-32 -right-20 size-80 rounded-full bg-emerald-100/60 blur-3xl" /><div className="pointer-events-none absolute -bottom-40 -left-20 size-96 rounded-full bg-orange-100/70 blur-3xl" /><div className="relative mx-auto grid w-full max-w-5xl gap-8 lg:grid-cols-[0.8fr_1.2fr] lg:items-center"><div className="hidden px-4 lg:block"><p className="font-tamil text-2xl text-primary">கற்றல் அனைவருக்கும்</p><h1 className="mt-5 font-display text-5xl font-extrabold leading-tight text-foreground">Learning feels closer in the language children know best.</h1><p className="mt-5 max-w-md text-base leading-relaxed text-muted-foreground">A welcoming space for teachers and students to learn, create, and grow together.</p></div><section className="rounded-3xl border border-border bg-card/95 p-6 shadow-xl sm:p-10"><Link href="/" className="flex w-fit items-center gap-2.5 rounded-xl focus-visible:outline-none focus-visible:ring-3 focus-visible:ring-ring/50"><span className="relative flex size-10 items-center justify-center rounded-2xl bg-primary text-primary-foreground"><BookOpenText className="size-5" /><Sparkles className="absolute -top-1 -right-1 size-4 text-accent" /></span><span className="flex flex-col leading-none"><span className="font-display text-lg font-bold">MozhiLearn</span><span className="font-tamil text-[11px] text-muted-foreground">மொழி கற்றல்</span></span></Link><div className="mt-8"><h2 className="font-display text-3xl font-extrabold text-foreground">{title}</h2><p className="mt-2 text-muted-foreground">{subtitle}</p></div><div className="mt-7">{children}</div><Link href="/" className="mt-7 block text-center text-sm font-semibold text-muted-foreground hover:text-primary">← Back to Home</Link></section></div></main>
}

export function Message({ children, error = false }: { children: React.ReactNode; error?: boolean }) { return <div role={error ? 'alert' : 'status'} className={`rounded-xl border p-3 text-sm ${error ? 'border-red-200 bg-red-50 text-red-700' : 'border-emerald-200 bg-emerald-50 text-emerald-700'}`}>{children}</div> }
export const inputClass = 'mt-2 min-h-12 w-full rounded-xl border border-input bg-background px-4 text-foreground outline-none transition focus:border-primary focus:ring-3 focus:ring-primary/15'